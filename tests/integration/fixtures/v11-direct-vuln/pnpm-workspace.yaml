# Integration fixture: pnpm 11 workspace with a vulnerable direct catalog
# dep. The user's `minimumReleaseAge: 720` MUST be restored verbatim after
# the run (the tool temporarily forces it to 0 to install fresh patches).
minimumReleaseAge: 720

packages:
  - 'apps/*'

catalog:
  lodash: '4.17.20'
